using UnityEngine;
using System.Collections;

// Texture Tool Extras (creates new processed texture)
// - Resize
// - Separate / combine / remove channels (can take alpha channel out to separate file, or combine to existing texture)
// - Color adjusts (invert, hue/brightness, ..)
// - Flip/Rotate?
// - 

public class TextureToolExtras : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
